<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="MyTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorBackground">@android:color/white</item>
        <item name="colorPrimary">@color/base</item>
        <item name="colorPrimaryDark">@color/base_dark</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <item name="colorControlNormal">@color/edittext_base</item>
        <item name="colorControlActivated">@color/edittext_highlighted</item>
        <item name="colorControlHighlight">@color/button_ripple</item>
        <item name="colorButtonNormal">@color/button_bg</item>
    </style>
    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>
    <style name="CustomPopupStyle" parent="Theme.AppCompat">
        <item name="android:textColor">@color/base_text_color</item>
    </style>
</resources>
